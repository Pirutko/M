MOSTIK v5.3.0 — unified patch from M-main(1)

BASE:
- Built from the uploaded M-main(1).zip.
- This source did NOT contain multi-role user support or microchips; both were added here.

CHANGED:
- public/app.js
  * multi-role switcher in header
  * admin can assign multiple roles
  * animal microchip on create/edit
  * animal profile editor with integrated photo editing
  * recurring calendar events
  * calendar is the single navigation surface for scheduled items + reminders
  * reminder recurrence UI (monthly, end date)
  * diet activation/period display and target calories
  * diet-backed food prefill
  * meal type dropdown: morning, before lunch, lunch, after lunch, dinner, treat, other
  * structured observation/ethogram fields
  * browser reminder notification deduplication

- netlify/functions/api.mjs
  * user_roles + active_role support
  * role switching API
  * multi-role admin management API
  * microchip persistence without uniqueness
  * animal profile PUT
  * recurring scheduled item fields and occurrence completion
  * diet target calories and active-plan handling
  * date-aware active diet meals
  * observation structured fields
  * reminder recurrence end date

- public/app.css
  * role switcher/admin role controls
  * integrated animal profile photo editor

NEW MIGRATIONS:
- 030_multi_roles.sql
- 031_animal_microchip.sql
- 032_recurring_calendar.sql
- 033_diet_assignments.sql
- 034_observation_ethogram.sql
- 035_reminder_schedule_end.sql

IMPORTANT:
- Microchip is intentionally NOT unique.
- Existing migrations were not rewritten; corrective/new schema changes are added as new migrations.
- Calendar recurring rules are stored once; future occurrences are generated by the UI rather than copied into hundreds of rows.
